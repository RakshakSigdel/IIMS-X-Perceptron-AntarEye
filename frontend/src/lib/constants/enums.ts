export enum UserRole {
  ADMIN = "admin",
  DOCTOR = "doctor",
}

export enum DiagnosisStatus {
  CREATED = "created",
  UPLOADED = "uploaded",
  PROCESSING = "processing",
  COMPLETED = "completed",
  FAILED = "failed",
}

export enum Gender {
  MALE = "male",
  FEMALE = "female",
  OTHER = "other",
}
